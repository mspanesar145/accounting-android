<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="searchViewStyle">@style/MySearchViewStyle</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="searchViewStyle">@style/MySearchViewStyle</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.NoActionBar.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.NoActionBar.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="CustomRatingSmall" parent="Base.Widget.AppCompat.RatingBar">
        <item name="android:progressDrawable">@drawable/rating_bar_small</item>
        <item name="android:minHeight">50dp</item>
        <item name="android:maxHeight">50dp</item>
    </style>

    <style name="MySearchViewStyle" parent="Widget.AppCompat.SearchView">
         <!--Background for the search query section (e.g. EditText)-->
        <item name="queryBackground">@drawable/search_view_background</item>
        <!--&lt;!&ndash; Background for the actions section (e.g. voice, submit) &ndash;&gt;-->
        <!--<item name="submitBackground">@android:color/transparent</item>-->
        <!-- Close button icon -->
        <item name="closeIcon">@drawable/ic_clear_white</item>
        <!-- Search button icon -->
        <item name="searchIcon">@drawable/ic_search_white</item>
        <!--&lt;!&ndash; Go/commit button icon &ndash;&gt;-->
        <!--<item name="goIcon">@drawable/ic_search_white</item>-->
        <!--&lt;!&ndash; Voice search button icon &ndash;&gt;-->
        <!--<item name="voiceIcon">...</item>-->
        <!--&lt;!&ndash; Commit icon shown in the query suggestion row &ndash;&gt;-->
        <!--<item name="commitIcon">@drawable/ic_search_white</item>-->
        <!--&lt;!&ndash; Layout for query suggestion rows &ndash;&gt;-->
        <!--<item name="suggestionRowLayout">...</item>-->
        <item name="searchHintIcon">@drawable/ic_search_white</item>
    </style>

</resources>
